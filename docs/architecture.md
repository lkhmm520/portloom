# Architecture

The current architecture guide has moved to [Architecture](/en/reference/architecture).

PortLoom's easy deployment uses a managed SSH service and Caddy on the public Docker host. Existing Caddy, Nginx, or Nginx Proxy Manager remains an optional advanced integration.

中文请参阅[系统架构](/reference/architecture)。
